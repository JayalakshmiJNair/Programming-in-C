#include<stdio.h>
#include<math.h>
void main(){
  int x,x1,x2,a,b,c;
  printf("x=(-b+/-sqrt(b^2-4ac)/2a)\n");
  printf("Enter a,b,c to find the value of x\n");
  scanf("%d%d%d",&a,&b,&c);
  x=sqrt(b*b-4*a*c);
  x1=(-b+x)/2*a;
  x2=(-b-x)/2*a;
  printf("x=%d,%d\n",x1,x2);
}
